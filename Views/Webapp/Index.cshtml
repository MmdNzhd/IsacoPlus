@model KaraYadak.Models.LandingViewModel
@{
    Layout = "_WebAppLanding";
    ViewData["Title"] = "صفحه اصلی";
}
@section Styles{
    <style>
        #swal2-content {
            color: #F44336;
        }

            #swal2-content > span > a {
                color: #007bff !important
            }

            #swal2-content > span {
                color: #545454 !important
            }
    </style>
}
<div id="throbber" style="display:none;">
    <img src="~/assets/iKSmF.gif" />
</div>
<div class="container m-0 mt-5 mtk-main p-0">
    <!--Banner-->
    <section class="sec-1 container-fluid py-2">
        <div class="row">
            <partial name="_WebAppHeader" />
        </div>
        <div class="row" data-0-top="opacity:0;left:100px;" data-200-top="opacity:1;left:0;">
            <!--Main Slider-->
            <div class="swiper-container main-slider animated fadeInDown">
                <div class="swiper-wrapper">
                    <div class="swiper-slide radius-10">
                        <a href="#" class="position-relative">
                            <img src="@Model.Slider_1" alt="Alternate Text" />
                            <div class="main-swiper-layer w-75">
                                <p>پارجه های جدید</p>
                                <p>مجلسی</p>
                                <p>در طرح ها و رنگ های مختلف</p>
                            </div>
                        </a>
                    </div>
                    <div class="swiper-slide radius-10">
                        <a href="#" class="position-relative">
                            <img src="@Model.Slider_2" alt="Alternate Text" />
                            <div class="main-swiper-layer w-75">
                                <p>پارجه های جدید</p>
                                <p>مجلسی</p>
                                <p>در طرح ها و رنگ های مختلف</p>
                            </div>
                        </a>
                    </div>
                    <div class="swiper-slide radius-10">
                        <a href="#" class="position-relative">
                            <img src="@Model.Slider_3" alt="Alternate Text" />
                            <div class="main-swiper-layer w-75">
                                <p>پارجه های جدید</p>
                                <p>مجلسی</p>
                                <p>در طرح ها و رنگ های مختلف</p>
                            </div>
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <h5 class="text-white text-right font-weight-bold py-4  ">دسته بندی مدل های پیشنهادی</h5>
    </section>
    <!--Category-->
    <div class="sec-2 container-fluid justify-content-around bg-white px-5 pt-4">
        <div class="mtk-category  rounded">
            <div class="row bg-white">
                @foreach (var item in Model.CategoryTypes)
                {
                    <div class="col-6 p-2 animated fadeInDown delay-1s">
                        <a asp-action="category" asp-route-type="@item.Id" class="w-100 h-100">
                            <img src="@item.Image" class="w-100" alt="Alternate Text" />
                            <p class="position-absolute">@item.Name</p>
                        </a>
                    </div>
                }
            </div>
        </div>

    </div>
    <!--Services-->
    <div class="sec-3 container-fluid bg-gray">
        <div class="row justify-content-between px-2">
            <div class="col-4 text-center p-0">
                <a href="#">
                    <img src="@Model.Services_1_Img" class="w-100 mtk-services wow fadeInDown" alt="Alternate Text" />
                    <div class="mtk-circle"></div>
                    <span class="text-center text-white d-inline-block py-2 animated wow fadeInDown px-3">@Model.Services_1</span>
                </a>
            </div>
            @*<div class="col-3 text-center p-0">
                    <a href="#">
                        <img src="@Model.Services_2_Img" class="w-100 mtk-services wow fadeInDown" alt="Alternate Text" />
                        <div class="mtk-circle"></div>
                        <span class="text-center text-white d-inline-block py-2 animated wow fadeInDown">@Model.Services_2</span>
                    </a>
                </div>*@
            <div class="col-4 text-center p-0">
                <a href="#">
                    <img src="@Model.Services_3_Img" width="120px" class="w-100 mtk-services wow fadeInDown" alt="Alternate Text" />
                    <div class="mtk-circle"></div>
                    <span class="text-center text-white d-inline-block py-2 animated wow fadeInDown px-3">@Model.Services_3</span>
                </a>
            </div>
            <div class="col-4 text-center p-0">
                <a href="#">
                    <img src="@Model.Services_4_Img" class="w-100 mtk-services wow fadeInDown" alt="Alternate Text" />
                    <div class="mtk-circle"></div>
                    <span class="text-center text-white d-inline-block py-2 animated wow fadeInDown px-3">@Model.Services_4</span>
                </a>
            </div>
        </div>
    </div>
    <!--Categories-->
    <div class="sec-4 container-fluid bg-white ltr">
        <h5 class="text-dark text-right p-2">دسته بندی ها</h5>
        <div class="swiper-container swiper-category h-100" id="category-1">
            <div class="swiper-wrapper">
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/skirt (2).svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
                <div class="swiper-slide h-100 wow fadeInDown">
                    <a href="#">
                        <img src="~/mtk/images/icon/gown.svg" alt="Alternate Text" />
                    </a>
                </div>
            </div>
        </div>
    </div>
    <!--Off Products-->
    <div class="sec-5 container-fluid bg-gray">
        <div class="row title align-items-center">
            <div class="col-8">
                <h5 class="text-white text-right py-3 position-relative sale mx-3"> تخفیف های امروز</h5>
            </div>
            <div class="col-4">
                <a href="#" class="text-white my-3">مشاهده همه ></a>
            </div>
        </div>
        <div class="row bg-white">
            <div class="swiper-container swiper-off ltr" id="off">
                <div class="swiper-wrapper">
                    @foreach (var item in Model.Specials)
                    {
                        <div class="swiper-slide position-relative p-2">

                            @*****************************************************************************************@
                            <div class="card position-relative wow fadeInDown" data-wow-duration="2s">
                                <a asp-action="productDetail" asp-route-code="@item.Product.Code" asp-route-name="@item.Product.Name">
                                @{ 
                                    var image = item.Product.ImageUrl == null ? "/assets/noImg.png" : item.Product.ImageUrl;
                                }
                                    <img class="card-img-top w-100" src="@image" alt="Card image cap">
                                    <div class="mtk-star position-absolute p-2" onclick="fav(this)">
                                        <i class="fa fa-star text-yellow mt-1"></i>
                                        <small class="text-white ml-3"> </small>
                                        <span style="color:white">3/5</span>
                                    </div>
                                    @if (User.Identity.Name != null)
                                    {
                                        <div data-title="@item.Product.Name @item.Categories?.Replace(",", "")" data-item-id="@item.Product.Code" style="cursor:pointer!important" class="mtk-like position-absolute p-2 m-2 mr-3" onclick="likeDislike(this, event)">
                                            @if (!Model.Favorites.Contains(item.Product.Code))
                                            {
                                                <i class="fa fa-heart text-white "></i>
                                            }
                                            else
                                            {
                                                <i class="fa fa-heart text-danger "></i>
                                            }
                                        </div>
                                    }
                                    <div class="mtk-off-perc position-absolute">
                                        @*<small class="text-dark"><span>3</span>% تخفیف</small>*@
                                        @if (item.Product.Discount != 0)
                                        {
                                            <span>
                                                <small class="font-weight-bold">@item.Product.Discount%</small>
                                            </span>
                                        }
                                        @*@if (item.NearEnd == true)
                                        {
                                            <span>
                                                <small>ته طاقه</small>
                                            </span>
                                        }*@
                                    </div>
                                    <div class="card-body p-0 px-2 py-1 bg-light-gray">
                                        <!-- Title -->
                                        <h6 class="card-title text-right p-0 m-0"><a>@item.Product.Name @item.Categories?.Replace(",", "")</a></h6>
                                        <!-- Text -->
                                        @*<span class="small d-inline-block w-100">مخصوص لباس مجلسی</span>*@
                                        <!-- Proce -->
                                        <span class="small d-inline-block w-100 p-0"> <span class="mtk-price">@string.Format("{0:n0}", item.Product.Price)</span>تومان</span>
                                        <!--Buy-->
                                        <div class="mtk-buy">
                                            @if (!ViewBag.Cookie.Contains(item.Product.Code.ToString()))
                                            {
                                                <a class="buyModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px" />
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="editModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px"/>
                                                </a>
                                            }
                                        </div>
                                    </div>
                                </a>
                            </div>
                            @**********************************************************************************@
                        </div>
                    }
                </div>
                <!-- Add Pagination -->
                <div class="swiper-pagination"></div>
            </div>
        </div>
    </div>

    <!--Slider-->

    <div class="sec-5 container-fluid bg-gray">
        <div class="row title align-items-center">
            <div class="col-8">
                <h5 class="text-white text-right py-3 position-relative">@Model.Product_Slider</h5>
            </div>
            <div class="col-4">
                <a href="#" class="text-white my-3">مشاهده همه ></a>
            </div>
        </div>
        <div class="row bg-white">
            <div class="swiper-container swiper-off ltr" id="slider-1">
                <div class="swiper-wrapper">
                    @foreach (var item in Model.Products)
                    {
                        <div class="swiper-slide position-relative p-2">

                            @*****************************************************************************************@
                            <div class="card position-relative wow fadeInDown" data-wow-duration="2s">
                                <a asp-action="productDetail" asp-route-code="@item.Product.Code" asp-route-name="@item.Product.Name">
                                    @{ 
                                        var image = item.Product.ImageUrl == null ? "/assets/noImg.png" : item.Product.ImageUrl;
                                    }
                                    <img class="card-img-top w-100" src="@image" alt="Card image cap">
                                    <div class="mtk-star position-absolute p-2" onclick="fav(this)">
                                        <i class="fa fa-star text-yellow mt-1"></i>
                                        <small class="text-white ml-3"> </small>
                                        <span style="color:white">3/5</span>
                                    </div>
                                    @if (User.Identity.Name != null)
                                    {
                                        <div data-title="@item.Product.Name @item.Categories?.Replace(",", "")" data-item-id="@item.Product.Code" style="cursor:pointer!important" class="mtk-like position-absolute p-2 m-2 mr-3" onclick="likeDislike(this, event)">
                                            @if (!Model.Favorites.Contains(item.Product.Code))
                                            {
                                                <i class="fa fa-heart text-white "></i>
                                            }
                                            else
                                            {
                                                <i class="fa fa-heart text-danger "></i>
                                            }
                                        </div>
                                    }
                                    <div class="mtk-off-perc position-absolute">
                                        @*<small class="text-dark"><span>3</span>% تخفیف</small>*@
                                        @if (item.Product.Discount != 0)
                                        {
                                            <span>
                                                <small class="font-weight-bold">@item.Product.Discount%</small>
                                            </span>
                                        }
                                        @*@if (item.NearEnd == true)
            {
                <span>
                    <small>ته طاقه</small>
                </span>
            }*@
                                    </div>
                                    <div class="card-body p-0 px-2 py-1 bg-light-gray">
                                        <!-- Title -->
                                        <h6 class="card-title text-right p-0 m-0"><a>@item.Product.Name @item.Categories?.Replace(",", "")</a></h6>
                                        <!-- Text -->
                                        @*<span class="small d-inline-block w-100">مخصوص لباس مجلسی</span>*@
                                        <!-- Proce -->
                                        <span class="small d-inline-block w-100 p-0"> <span class="mtk-price">@string.Format("{0:n0}", item.Product.Price)</span>تومان</span>
                                        <!--Buy-->
                                        <div class="mtk-buy">
                                            @if (!ViewBag.Cookie.Contains(item.Product.Code.ToString()))
                                            {
                                                <a class="buyModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px"/>
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="editModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px" />
                                                </a>
                                            }
                                        </div>
                                    </div>
                                </a>
                            </div>
                            @**********************************************************************************@
                        </div>
                    }

                </div>
                <!-- Add Pagination -->
                <div class="swiper-pagination"></div>
            </div>
        </div>
    </div>

    <!--Slider 2-->

    <div class="sec-5 container-fluid bg-gray mb-4">
        <div class="row title align-items-center">
            <div class="col-8">
                <h5 class="text-white text-right py-3 position-relative">@Model.Product_Slider_2</h5>
            </div>
            <div class="col-4">
                <a href="#" class="text-white my-3">مشاهده همه ></a>
            </div>
        </div>
        <div class="row bg-white">
            <div class="swiper-container swiper-off ltr" id="slider-2">
                <div class="swiper-wrapper">

                    @foreach (var item in Model.Products2)
                    {
                        <div class="swiper-slide position-relative p-2">

                            @*****************************************************************************************@
                            <div class="card position-relative wow fadeInDown" data-wow-duration="2s">
                                <a asp-action="productDetail" asp-route-code="@item.Product.Code" asp-route-name="@item.Product.Name">
                                    @{ 
                                        var image = item.Product.ImageUrl == null ? "/assets/noImg.png" : item.Product.ImageUrl;
                                    }
                                    <img class="card-img-top w-100" src="@image" alt="Card image cap">
                                    <div class="mtk-star position-absolute p-2" onclick="fav(this)">
                                        <i class="fa fa-star text-yellow mt-1"></i>
                                        <small class="text-white ml-3"> </small>
                                        <span style="color:white">3/5</span>
                                    </div>
                                    @if (User.Identity.Name != null)
                                    {
                                        <div data-title="@item.Product.Name @item.Categories?.Replace(",", "")" data-item-id="@item.Product.Code" style="cursor:pointer!important" class="mtk-like position-absolute p-2 m-2 mr-3" onclick="likeDislike(this, event)">
                                            @if (!Model.Favorites.Contains(item.Product.Code))
                                            {
                                                <i class="fa fa-heart text-white "></i>
                                            }
                                            else
                                            {
                                                <i class="fa fa-heart text-danger "></i>
                                            }
                                        </div>
                                    }
                                    <div class="mtk-off-perc position-absolute">
                                        @*<small class="text-dark"><span>3</span>% تخفیف</small>*@
                                        @if (item.Product.Discount != 0)
                                        {
                                            <span>
                                                <small class="font-weight-bold">@item.Product.Discount%</small>
                                            </span>
                                        }
                                        @*@if (item.NearEnd == true)
                                        {
                                            <span>
                                                <small>ته طاقه</small>
                                            </span>
                                        }*@
                                    </div>
                                    <div class="card-body p-0 px-2 py-1 bg-light-gray">
                                        <!-- Title -->
                                        <h6 class="card-title text-right p-0 m-0"><a>@item.Product.Name @item.Categories?.Replace(",", "")</a></h6>
                                        <!-- Text -->
                                        @*<span class="small d-inline-block w-100">مخصوص لباس مجلسی</span>*@
                                        <!-- Proce -->
                                        <span class="small d-inline-block w-100 p-0"> <span class="mtk-price">@string.Format("{0:n0}", item.Product.Price)</span>تومان</span>
                                        <!--Buy-->
                                        <div class="mtk-buy">
                                            @if (!ViewBag.Cookie.Contains(item.Product.Code.ToString()))
                                            {
                                                <a class="buyModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px" />
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="editModal @item.Product.Code-00" data-item-id="@item.Product.Code" href="#">
                                                    <img src="~/mtk/images/icon/redCart.svg" width="30px" />
                                                </a>
                                            }
                                        </div>
                                    </div>
                                </a>
                            </div>
                            @**********************************************************************************@
                        </div>
                    }


                    
                </div>
                <!-- Add Pagination -->
                <div class="swiper-pagination"></div>
            </div>
        </div>
    </div>
    <!--Menu-->
    <partial name="_Menu" />

    <!-- Search Modal -->
    <!-- Modal -->
    <div class="modal fade" id="buy" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
         aria-hidden="true">

        <!-- Add .modal-dialog-centered to .modal-dialog to vertically center the modal -->
        <div class="modal-dialog modal-dialog-centered" role="document">


            <div class="modal-content  border border-yellow">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">خرید پارچه</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        @*<span aria-hidden="true">&times;</span>*@
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-group text-right float-right w-50">
                        <label for="meter">متر</label>
                        <select class="form-control" id="meter">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option>3</option>
                            <option>4</option>
                            <option>6</option>
                            <option>7</option>
                            <option>8</option>
                        </select>
                    </div>
                    <div class="form-group text-right float-right w-50">
                        <label for="centimiter">سانتی متر</label>
                        <select class="form-control" id="centimiter">
                            <option value="0">0</option>
                            <option value="10">10</option>
                            <option>20</option>
                            <option>40</option>
                            <option>60</option>
                            <option>80</option>
                        </select>
                    </div>

                </div>
                <div id="modalFooter" class="modal-footer align-items-center">
                    <button type="button" class="btn btn-red addToBascket" data-item-id=""><i class="fas fa-shopping-cart mt-2"></i><span>افزودن به سبد خرید</span></button>
                </div>
            </div>
        </div>
    </div>

    @* ********************************************************** *@
    <div class="modal fade" id="edit" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle"
         aria-hidden="true">

        <!-- Add .modal-dialog-centered to .modal-dialog to vertically center the modal -->
        <div class="modal-dialog modal-dialog-centered" role="document">


            <div class="modal-content  border border-yellow">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">خرید پارچه</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        @*<span aria-hidden="true">&times;</span>*@
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-group text-right float-right w-50">
                        <label for="meter">متر</label>
                        <select class="form-control" id="eMeter">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option>3</option>
                            <option>4</option>
                            <option>6</option>
                            <option>7</option>
                            <option>8</option>
                        </select>
                    </div>
                    <div class="form-group text-right float-right w-50">
                        <label for="centimiter">سانتی متر</label>
                        <select class="form-control" id="eCentimiter">
                            <option value="0">0</option>
                            <option value="10">10</option>
                            <option>20</option>
                            <option>40</option>
                            <option>60</option>
                            <option>80</option>
                        </select>
                    </div>

                </div>
                <div id="modalFooter" class="modal-footer align-items-center">
                    <button type="button" class="btn btn-red changeLength" data-item-id=""><i class="fas fa-shopping-cart mt-2"></i><span>تغییر متراژ</span></button>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
    <script>
        //$.cookie("one", "two", { path: '/' })
        var products = [];
        var goods = [];
        var modals = [];
        $(".buyModal").on("click", function (e) {
            e.preventDefault();
            if (modals.includes($(this).attr("data-item-id"))) {
                $(".changeLength").attr("data-item-id", $(this).attr("data-item-id"));
                $("#edit").modal();
            } else {
                $(".addToBascket").attr("data-item-id", $(this).attr("data-item-id"));
                $("#buy").modal();
            }
        });
        $(".editModal").on("click", function (e) {
            e.preventDefault();
            $(".changeLength").attr("data-item-id", $(this).attr("data-item-id"));
            $("#edit").modal();
        });
        $(".addToBascket").on("click", function (e) {
            e.preventDefault();
            console.log($(this).attr('data-target', "#edit"));
            var meter = parseInt($("#meter option:selected").text() * 100);
            var centimeter = parseInt($("#centimiter option:selected").text());
            var val = meter + centimeter;
            var oldCookieValue = $.cookie('cart');
            var pId = $(this).attr("data-item-id") + "-" + val;
            products.push(pId);
            if (oldCookieValue == undefined) {
                $.cookie('cart', products.join('_'), { expires: 7, path: '/' });
            } else {
                var value = oldCookieValue + "_" + products.join('_');
                $.removeCookie('cart', { path: '/' });
                //Create new cookie with same name and concatenate the old and new desired value.
                $.cookie('cart', value, { expires: 7, path: '/' });
            }
            products = [];
            $('#buy').modal('toggle');
            $("." + $(this).attr("data-item-id") + "-00").addClass("editModal").removeClass("buyModal");
            modals.push($(this).attr("data-item-id"));
            let currentBascketCount = $('#cartCount').text();
            currentBascketCount = Number(currentBascketCount) + 1;
            $('#cartCount').text(currentBascketCount);
        });
        $(".changeLength").on("click", function (e) {
            e.preventDefault();
            var meter = parseInt($("#eMeter option:selected").text() * 100);
            var centimeter = parseInt($("#eCentimiter option:selected").text());
            var val = meter + centimeter;
            var oldCookieValue = $.cookie('cart');
            $.ajax({
                type: 'POST',
                url: '/webapp/ChangeLength',
                data: {
                    id: $(this).attr("data-item-id"),
                    length: val
                }
            }).done(function (result) {
                if (result.status != '0') {
                    $.removeCookie('cart', { path: '/' });
                    $.cookie("cart", result, { expires: 7, path: '/' });
                    $("#edit").modal('toggle');
                }
            });
        });
        function likeDislike(item, e) {
            e.preventDefault();
            if ($(item).children('svg').hasClass('text-white')) {
                $.blockUI({ message: $('#throbber') });
                $(item).children('svg').animate().addClass('text-danger');
                $(item).children('svg').attr('transform', 'scale(1.3)');
                $(item).children('svg').animate().removeClass('text-white');
                var value = $(item).attr("data-item-id");
                var product = $(item).attr("data-title");
                $.ajax({
                    type: 'POST',
                    url: '/webapp/AddToFavorite',
                    data: {
                        Id: value
                    }
                }).done(function (result) {
                    if (result.status != '0') {
                        //swal("", result.message, "success");
                        Swal.fire({
                            icon: "success",
                            html: "</span>" + product + '<span> به لیست علاقه مندی شما اضافه شد. جهت مشاهده لیست علاقه مندی خود</a> اینجا <a  href="/webapp/Favorite  "> کلیک کنید ',
                            confirmButtonText: "بستن",
                        });
                    }
                });
                $.unblockUI();

            }
            else/* if ($(item).children('svg').hasClass('text-danger')) */ {
                $.blockUI({ message: $('#throbber') });
                $(item).children('svg').animate().addClass('text-white');
                $(item).children('svg').attr('transform', 'scale(1)');
                $(item).children('svg').animate().removeClass('text-danger');
                var value = $(item).attr("data-item-id");
                $.ajax({
                    type: 'POST',
                    url: '/webapp/RemoveFromFavorites',
                    data: {
                        Id: value
                    }
                }).done(function (result) {
                    if (result.status != '0') {
                        //swal("", result.message, "success");
                    }
                    console.log(result);
                });
                $.unblockUI();
            }
        }
    </script>
}