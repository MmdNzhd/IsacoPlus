@model KaraYadak.Components.PageHeaderComponentVM

<div class="content-header row">
    <div class="content-header-left col-md-9 col-12 mb-2">
        <div class="row breadcrumbs-top">
            <div class="col-12">
                <h2 class="content-header-title float-left mb-0">@Model.Page</h2>
                <div class="breadcrumb-wrapper col-12">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/">داشبورد</a></li>
                        @if (Model.Parent != "")
                        {
                        <li class="breadcrumb-item"><a href="@Url.Content(Model.ParentUrl)">@Model.Parent</a></li>
                        }
                        <li class="breadcrumb-item active">@Model.Page</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
</div>